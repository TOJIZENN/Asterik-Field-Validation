# Asterik-Field-Validation

Created a React component that allows users to input and validate
required fields for Name and Location. It displays a red
asterisk for mandatory fields, highlight validation errors when
fields are left empty, and show a success message with
submitted values when both fields are filled correctly.
